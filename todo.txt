Initial setup
=============
Buttons:
- Load previous setup
- Save current setup
- Participating countries list
    - Updates on finish every line entry
    - Saves as an ordered list
- Logo chooser
    - Attempt native file chooser
    - Say recommend file size has height 140 px
- Logo Preview
    - Image viewer
- Conference Name
    - Updates on lose focus
- Committee Name
    - Updates on lose focus
- Color chooser
    - 3 of them for primary, accent, accent 2
- Screen mode
    - Choose between functions button
    - Immediately changes creen

Classes required
==================
Setup:

- get/set for all the attributes
- Config file saver
    - creates file with all attributes

todo:
- Screen chooser
    - possible modes are off, black, logo, roll call, session, unmod
- Logo chooser
    - Saves absolute path
    - Makes sure that file is png, jpg, or gif
- Logo displayer
    - args: max width, max height, file path
- Config file loader
    - opens up a file browser to find the file
- Config file parser
    - args: path of config file
    - uses set function of each attribute
- Config file saver
    - opens up a file browser to choose save location

lines.add("<MUNConfigFile>");
        lines.add("logoPath=" + logoPath);
        lines.add("conferenceName=" + conferenceName);
        lines.add("committeeName=" + committeeName);
        lines.add("countriesList=" + countriesList.toString());
        lines.add("primaryColor=" + primaryColor.getRed() + ", "
                + primaryColor.getGreen() + ", " + primaryColor.getBlue()
                + ", " + primaryColor.getOpacity());
        lines.add("firstAccent=" + firstAccent.getRed() + ", "
                + firstAccent.getGreen() + ", " + firstAccent.getBlue()
                + ", " + firstAccent.getOpacity());
        lines.add("secondAccent=" + secondAccent.getRed() + ", "
                + secondAccent.getGreen() + ", " + secondAccent.getBlue() + ", "
                + secondAccent.getOpacity());
        lines.add("topic=" + topic);
        lines.add("numberOfSpeakers=" + numberOfSpeakers);
        lines.add("sessionMode=" + sessionMode);
        lines.add("currentSpeaker=" + currentSpeaker);
        lines.add("speakersList=" + speakersList.toString());
        lines.add("resolutionTitle=" + resolutionTitle);
        lines.add("totalTime=" + totalTimeSec);
        lines.add("timePerSpeaker=" + timePerSpeakerSec);
        lines.add("</MUNConfigFile>");